#include <stdio.h>
void main (){
    float r , Area ; 

    printf("Enter the Radius of a circle ");//Take radius input from user
    scanf("%f",r);
    Area=3.142*r*r; //Calculate area using formula, pie*r^2
    printf("the area of the Circle is %f",Area);

    
    
}